{
	"name": "headfwd-proxy",
	"version": "0.0.0",
	"private": true,
	"description": "Cloudflare Workers + Durable Objects reverse-tunnel proxy for HeadFwd",
	"license": "MIT",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.8.19",
		"typescript": "^5.5.2",
		"vitest": "~3.2.0",
		"wrangler": "^4.54.0"
	},
	"dependencies": {}
}
